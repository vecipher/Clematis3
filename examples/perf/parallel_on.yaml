# Tiny example to enable parallelism for smoke; no perf thresholds, metrics off.
perf:
  enabled: true
  metrics:
    report_memory: false
  parallel:
    enabled: true
    max_workers: 2
    t1: true
    t2: true
    agents: true
