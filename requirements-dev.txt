# Minimal, pinned dev toolchain for CI/lint (keep in sync with pyproject optional-deps)
ruff==0.13.3
mypy==1.11.2
pre-commit==3.7.1
pytest==8.3.2
pytest-cov==5.0.0
types-PyYAML==6.0.12.20240808

# Notes:
# - Runtime/test deps (numpy, zstandard, pyyaml, etc.) are provided via project metadata/extras.
# - Keep this file minimal to avoid resolver drift; lint workflow installs only these pins.
