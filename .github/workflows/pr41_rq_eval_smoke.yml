name: PR41 RQ Eval Smoke

on:
  pull_request:
    paths:
      - 'scripts/rq_eval.py'
      - 'tests/fixtures/rq/**'
      - 'tests/scripts/test_rq_eval_cli.py'
      - 'tests/integration/test_rq_eval_end_to_end.py'
      - '.github/workflows/pr41_rq_eval_smoke.yml'
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Export GIT SHA
        run: |
          echo "CLEMATIS_GIT_SHA=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install deps
        run: |
          python -m pip install -U pip wheel
          if [ -f requirements.txt ]; then
            python -m pip install -r requirements.txt
          else
            python -m pip install pytest hypothesis pyyaml
          fi
          if [ -f pyproject.toml ] || [ -f setup.py ]; then
            python -m pip install -e .
          fi

      - name: Run PR41 smoke tests
        run: |
          pytest -q tests/scripts/test_rq_eval_cli.py::test_metric_functions_on_toy_set
          pytest -q tests/scripts/test_rq_eval_cli.py::test_cli_produces_json_and_csv
          pytest -q tests/integration/test_rq_eval_end_to_end.py

      - name: Upload artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: pr41-rq-eval-smoke
          path: |
            logs/quality/**
            .pytest_cache/**
            out/**
          if-no-files-found: ignore